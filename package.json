{
  "name": "trowel",
  "version": "0.0.1",
  "private": true,
  "description": "Schema-driven desktop editor for the pixelbrix.com content collections.",
  "scripts": {
    "dev": "cd src-tauri && PATH=\"$HOME/.cargo/bin:$PATH\" cargo tauri dev",
    "build": "cd src-tauri && PATH=\"$HOME/.cargo/bin:$PATH\" cargo tauri build"
  }
}
