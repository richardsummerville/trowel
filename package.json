{
  "name": "trowel",
  "version": "0.0.1",
  "private": true,
  "description": "Schema-driven desktop editor for the pixelbrix.com content collections.",
  "scripts": {
    "dev": "cd src-tauri && cargo tauri dev",
    "build": "cd src-tauri && cargo tauri build"
  }
}
